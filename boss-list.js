(()=>{const KEY='boss-creator-v1';const spr=id=>`https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/${id}.png`;const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));const sprite=p=>{const n=window.POKEMON_DATA?.find(x=>x.name===p.name);return spr(Number(n?.id)||1)};function enhance(){const grid=document.querySelector('.boss-grid');if(!grid)return;const active=document.querySelector('.save-item.active');if(!active)return;let s;try{s=JSON.parse(localStorage.getItem(KEY)||'{"games":[]}')}catch{return}const g=s.games.find(x=>x.id===active.dataset.game);if(!g)return;grid.className='boss-list';grid.innerHTML=g.bosses.length?g.bosses.map(b=>`<article class="boss-list-card" data-boss="${b.id}"><div class="boss-list-photo">${b.photo?`<img src="${b.photo}" alt="">`:'<div class="boss-placeholder">👤</div>'}</div><div class="boss-list-main"><div class="boss-list-title"><span class="badge">${esc(b.type)}</span><h3>${esc(b.name)}</h3></div><div class="boss-list-team">${(b.pokemon||[]).map(p=>`<img class="boss-mini-sprite" src="${sprite(p)}" alt="${esc(p.name)}" title="${esc(p.name)}">`).join('')}</div><div class="boss-list-cap"><span>LVL CAP</span><strong>${Math.max(0,...(b.pokemon||[]).map(p=>Number(p.level)||0))||'—'}</strong></div></div></article>`).join(''):`<div class="card" style="text-align:center"><h3>Aún no hay bosses</h3></div>`;grid.querySelectorAll('[data-boss]').forEach(el=>el.addEventListener('click',()=>{const id=el.dataset.boss;const native=window.render;const st=JSON.parse(localStorage.getItem(KEY)||'{"games":[]}');const game=st.games.find(x=>x.id===active.dataset.game);if(game?.bosses.some(b=>b.id===id)){sessionStorage.setItem('bossCreatorOpenBoss',id);location.reload()}}))}const native=window.render;if(typeof native==='function'){window.render=()=>{native();setTimeout(enhance,0)}}setTimeout(enhance,50)})();